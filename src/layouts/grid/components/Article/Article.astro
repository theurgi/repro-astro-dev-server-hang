<article>
	<slot />
</article>

<style>
	article {
		/* max-width: var(--article-max-width); */
		min-height: 130vh;
		padding: var(--article-padding-y) var(--article-padding-x);
		margin: 0 auto;
	}
</style>
